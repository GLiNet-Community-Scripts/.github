# GL.iNet Community Scripts Org Profile

This repository manages the GitHub organization profile for `GLiNet-Community-Scripts`.

- `profile/README.md` is the public-facing organization homepage.
- `.github/workflows/update-org-profile.yml` updates the profile automatically.
- `scripts/generate-profile-readme.mjs` fetches all public organization repositories and renders the README.
